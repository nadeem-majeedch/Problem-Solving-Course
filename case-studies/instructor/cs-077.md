# cs-077 — The 4-Digit Lock (instructor)

| Field | Value |
| --- | --- |
| id | cs-077 |
| difficulty | Beginner |
| topics | brute force, search space |
| lecture | lecture-20 |
| estimated time | 5-minute attempt + 10-minute discussion |
| prerequisites | none |
| objective | state inputs, outputs, and assumptions, then solve a small instance by hand |

> **Instructor only.** Do not distribute to students or publish on the
> public site. The public build excludes this file and its text.

## Reasoning walkthrough

**Worked instance.** 4-digit PINs, digits 0-9, digits sum to 12

**Expected result.** count first: 10^4 = 10000 candidates; favourable = 415 (455 sums possible minus the four all-identical digits)

**Method — brute force and search spaces.** Walk the class through these moves:

1. Describe the search space precisely (what is one candidate?).
2. Count the space before enumerating it.
3. Enumerate systematically to avoid duplicates and gaps.
4. Filter with the constraint; prune what cannot succeed.

**Where this class gets stuck.** Count the space BEFORE coding - 10,000 is trivially enumerable; the lesson is the arithmetic, not the loop. Expect the first
five minutes to produce enumerating unordered items in every order (n! instead of c(n,k)) as the
most common wrong turn; plan the reveal around it.


## Five-minute teaching sequence

Minute 0–1 — **Read and analyse.** Expect inputs, outputs, and at least
one stated assumption named; the deciding quantity here is the input
rule. Watch for: retelling the story without extracting the decision.
Minute 1–3 — **Develop an approach.** Expect a rule or a first-step
computation on the instance; a correct rule with an arithmetic slip is
partial credit. Watch for: mutating the input collection the caller still holds while scanning it.
Minute 3–5 — **Compare and write.** Expect the update rule or loop
skeleton written down, pairs comparing orders of operations. If most
pairs stall, give the structure hint from the difficulty ladder
(below) whole-class rather than pair by pair.
Reveal and discuss — keep the reveal inside 10 minutes so the exit question still fits.


## Difficulty adaptation

- **Simplify.** Reduce the space (three candidates instead of the full set) and enumerate by hand. Then shrink the instance: run only the first
  step of 4-digit PINs, digits 0-9, digits sum to 12 and enlarge after success.
- **Extend.** Bound the candidate space first; then decide whether enumeration is honest.
- **Hints (progressive).**
  1. *Re-orient:* "Describe the search space precisely (what is one candidate?)."
  2. *Structure:* "Bound the space first: count the candidates before generating any of them."
  3. *Near-answer:* the common wrong turn is 'enumerating unordered items in every order (n! instead of c(n,k))' — undo
     that choice and re-derive the step it breaks.


## Reference solution (pseudocode)

FUNCTION pins_sum_12()
    hits <- []
    FOR EACH pin IN range 0..10000 DO
        IF SUM({INTEGER(d) for each d in <formatted text>}) = 12 THEN
            append pin to hits
    RETURN hits
// demo run: OUTPUT LENGTH(hits), hits[..5]


## Reference solution (Python)

```python
def pins_sum_12():
    hits = []
    for pin in range(10000):
        if sum(int(d) for d in f"{pin:04d}") == 12:
            hits.append(pin)
    return hits

if __name__ == "__main__":
    hits = pins_sum_12()
    print(len(hits), hits[:5])
```

The code is runnable as-is; executing the file prints the worked instance
so you can demo it live on the projector.


## Edge cases

- Empty or single-item input: decide what is returned and what must NOT be printed.
- Degenerate values: zero, negatives, and duplicates among the instance's data.


## Alternative approaches

- Count the space BEFORE coding - 10,000 is trivially enumerable; the lesson is the arithmetic, not the loop.
- Loop-first formulation versus a comprehension/direct-expression formulation — same result, different emphasis on state versus intent.
- A table filled by hand before any code: for the size of instance here it is both a solution and the test oracle.
- **Cost.** Linear: one visit per item, a constant amount of work per visit; the alternatives shift exactly this cost.


## Common pitfalls

- Enumerating unordered items in every order (n! instead of c(n,k)).
- Mutating the caller's list when the result was meant to be a fresh one — the demo where the input 'changes by itself'.
- Reporting the answer without the rule that produced it — the decision becomes uncheckable.


## Reveal script

1. Display the case and start the timer (5 minutes). While students work,
   circulate and read shoulders; pick one conventional attempt and one
   surprising one for the board.
2. Ask first: 'What is the search space, and in what order will you try it?'
3. Walk the worked instance (4-digit PINs, digits 0-9, digits sum to 12) on the board
   under the stated rule before any code appears; the class calls the
   next state at each step.
4. Surface the wrong turn directly: 'enumerating unordered items in every order (n! instead of c(n,k))' — ask the room
   which quiet assumption it makes.
5. Show the reference Python, run it on the worked instance, and let the
   class compare with their own answers.
6. Close by naming the technique — "brute force and search spaces" — and hand over the
   discussion prompt: **"Which constraint is cheapest to check first, and why does order matter?"**


## Assessment use

- **Formative use.** discussion-grade: mark assumption quality and rule precision rather than the final number, and expect the full cycle to run long.
- **Mini-rubric (10 points).**
  - *Structure (2):* inputs, outputs, and the binding constraint named before any computation.
  - *Method (3):* the stated moves executed — especially "Count the space before enumerating it"
    and "Enumerate systematically to avoid duplicates and gaps".
  - *Result (3):* the worked instance computed correctly (count first: 10^4 = 10000 candidates; favourable = 415 (455…),
    and the answer survives the smallest and the boundary input.
  - *Communication (2):* assumptions stated; the answer reports which rule produced it.
- **Follow-up check.** State the deciding rule in one sentence and apply it to a half-size version of the worked instance.


## Connections

- Discussion prompt for after the reveal: **"Which constraint is cheapest to check first, and why does order matter?"**
- This case belongs to lecture-20 as case cs-077 of four; it
  practises brute force, search space at beginner level.
- Case before: none (first of the lecture);
  case after: cs-078.


## Extension challenge

Bound the candidate space first; then decide whether enumeration is honest.

Offer this only after the main cycle lands; it is enrichment, not
required content, and works as homework for fast finishers.

