# cs-004 — The Library Fine Formula (instructor)

| Field | Value |
| --- | --- |
| id | cs-004 |
| difficulty | Expert |
| topics | specification, edge cases |
| lecture | lecture-01 |
| estimated time | 5-minute attempt + 10-minute discussion |
| prerequisites | the full Block I-III toolbox and proof-shaped reasoning |
| objective | make and justify design decisions where the method itself is the challenge |

> **Instructor only.** Do not distribute to students or publish on the
> public site. The public build excludes this file and its text.

## Reasoning walkthrough

**Worked instance.** 0.50/day for days 1-7, 1.00/day for days 8-30, cap 10.00; returned on day 5, on day 12, on day 40

**Expected result.** 2.50; 7.00 (5 charged days: 7+5x1.00); 10.00 (capped); same-day return 0.00

**Method — modelling, rules, and decisions.** Walk the class through these moves:

1. Extract the entities, quantities, and rules from the story.
2. State assumptions explicitly (what is fixed, what varies).
3. Write the decision procedure as precise rules.
4. Run the rules on the worked instance; sanity-check the output.

**Where this class gets stuck.** The ambiguity to resolve first: is the return day itself charged? State the decision, then compute. Expect the first
five minutes to produce hiding a value judgement inside 'obvious' arithmetic as the
most common wrong turn; plan the reveal around it.


## Five-minute teaching sequence

Minute 0–1 — **Read and analyse.** Expect inputs, outputs, and at least
one stated assumption named; the deciding quantity here is the input
rule. Watch for: retelling the story without extracting the decision.
Minute 1–3 — **Develop an approach.** Expect a rule or a first-step
computation on the instance; a correct rule with an arithmetic slip is
partial credit. Watch for: rounding mid-computation on the input values instead of once at the end.
Minute 3–5 — **Compare and write.** Expect the update rule or loop
skeleton written down, pairs comparing orders of operations. If most
pairs stall, give the structure hint from the difficulty ladder
(below) whole-class rather than pair by pair.
Reveal and discuss — keep the reveal inside 10 minutes so the exit question still fits.


## Difficulty adaptation

- **Simplify.** Give the rule list with one line blank and let the class compute that line together. Then shrink the instance: run only the first
  step of 0.50/day for days 1-7, 1.00/day for days 8-30, cap 10.00;… and enlarge after success.
- **Extend.** Re-solve with a second, different assumption and compare the answers.
- **Hints (progressive).**
  1. *Re-orient:* "Extract the entities, quantities, and rules from the story."
  2. *Structure:* "Turn the story into entities, quantities, and one precise rule before computing anything."
  3. *Near-answer:* the common wrong turn is 'hiding a value judgement inside 'obvious' arithmetic' — undo
     that choice and re-derive the step it breaks.


## Reference solution (pseudocode)

FUNCTION fine(days_late, cap)
    IF days_late <= 0 THEN
        RETURN 0.0
    total <- MIN(days_late, 7) * 0.5
    IF days_late > 7 THEN
        total <- total + (MIN(days_late, 30) - 7) * 1.0
    RETURN MIN(total, cap)


## Reference solution (Python)

```python
def fine(days_late, cap=10.00):
    """Library fine: 0.50/day for 1-7, 1.00/day for 8-30, capped.
    Decision: the return day itself is NOT charged (late days are 1..days_late)."""
    if days_late <= 0:
        return 0.00
    total = min(days_late, 7) * 0.50
    if days_late > 7:
        total += (min(days_late, 30) - 7) * 1.00
    return min(total, cap)

if __name__ == "__main__":
    for d in (0, 5, 12, 40):
        print(d, fine(d))
```

The code is runnable as-is; executing the file prints the worked instance
so you can demo it live on the projector.


## Edge cases

- Empty or single-item input: decide what is returned and what must NOT be printed.
- Degenerate values: zero, negatives, and duplicates among the instance's data.
- Rounding: say where values are rounded and why a last digit may differ between two correct attempts.


## Alternative approaches

- The ambiguity to resolve first: is the return day itself charged? State the decision, then compute.
- Loop-first formulation versus a comprehension/direct-expression formulation — same result, different emphasis on state versus intent.
- A table filled by hand before any code: for the size of instance here it is both a solution and the test oracle.
- **Cost.** Constant: once the rules are fixed, the fine is a closed-form expression evaluated once — the work is in writing the rules, not running them; the alternatives shift exactly this cost.


## Common pitfalls

- Hiding a value judgement inside 'obvious' arithmetic.
- Comparing floating-point values for exact equality instead of a tolerance (or rounding at the very end, not mid-computation).
- Reporting the answer without the rule that produced it — the decision becomes uncheckable.


## Reveal script

1. Display the case and start the timer (5 minutes). While students work,
   circulate and read shoulders; pick one conventional attempt and one
   surprising one for the board.
2. Ask first: 'Which quantity does the rule attach to, and what exactly does one application of the rule change?'
3. Walk the worked instance (0.50/day for days 1-7, 1.00/day for days 8-30, cap 10.00; returned on day 5, on day 12,…) on the board
   under the stated rule before any code appears; the class calls the
   next state at each step.
4. Surface the wrong turn directly: 'hiding a value judgement inside 'obvious' arithmetic' — ask the room
   which quiet assumption it makes.
5. Show the reference Python, run it on the worked instance, and let the
   class compare with their own answers.
6. Close by naming the technique — "modelling, rules, and decisions" — and hand over the
   discussion prompt: **"Which assumption would you attack first if you had to argue the other side?"**


## Assessment use

- **Formative use.** discussion-grade: mark assumption quality and rule precision rather than the final number, and expect the full cycle to run long.
- **Mini-rubric (10 points).**
  - *Structure (2):* inputs, outputs, and the binding constraint named before any computation.
  - *Method (3):* the stated moves executed — especially "State assumptions explicitly (what is fixed, what varies)"
    and "Write the decision procedure as precise rules".
  - *Result (3):* the worked instance computed correctly (2.50; 7.00 (5 charged days: 7+5x1.00); 10.00 (capped); same-day…),
    and the answer survives the smallest and the boundary input.
  - *Communication (2):* assumptions stated; the answer reports which rule produced it.
- **Follow-up check.** Where does rounding happen, and which digit may differ between two correct implementations?


## Connections

- Discussion prompt for after the reveal: **"Which assumption would you attack first if you had to argue the other side?"**
- This case belongs to lecture-01 as case cs-004 of four; it
  practises specification, edge cases at intermediate level.
- Case before: cs-003;
  case after: none (last of the lecture).


## Extension challenge

Re-solve with a second, different assumption and compare the answers.

Offer this only after the main cycle lands; it is enrichment, not
required content, and works as homework for fast finishers.

