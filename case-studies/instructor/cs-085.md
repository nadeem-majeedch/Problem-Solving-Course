# cs-085 — The Russian Dolls (instructor)

| Field | Value |
| --- | --- |
| id | cs-085 |
| difficulty | Beginner |
| topics | recursion, base case |
| lecture | lecture-22 |
| estimated time | 5-minute attempt + 10-minute discussion |
| prerequisites | none |
| objective | state inputs, outputs, and assumptions, then solve a small instance by hand |

> **Instructor only.** Do not distribute to students or publish on the
> public site. The public build excludes this file and its text.

## Reasoning walkthrough

**Worked instance.** doll with 5 layers

**Expected result.** count_layers: 0 for no doll, else 1 + inner; base case is the whole lesson

**Method — recursion and dynamic programming.** Walk the class through these moves:

1. Define the subproblem in words (what does f(k) mean?).
2. Find the base case(s) — the smallest inputs answered directly.
3. Write the transition: how f(k) uses smaller answers.
4. Draw the call tree; memoise if subproblems repeat.

**Where this class gets stuck.** Write the base case in one sentence: 'a doll with nothing inside has 1 layer'. Expect the first
five minutes to produce a missing or wrong base case = infinite recursion as the
most common wrong turn; plan the reveal around it.


## Five-minute teaching sequence

Minute 0–1 — **Read and analyse.** Expect inputs, outputs, and at least
one stated assumption named; the deciding quantity here is the input
rule. Watch for: retelling the story without extracting the decision.
Minute 1–3 — **Develop an approach.** Expect a rule or a first-step
computation on the instance; a correct rule with an arithmetic slip is
partial credit. Watch for: solving the big instance first instead of naming the base case for the smallest input.
Minute 3–5 — **Compare and write.** Expect the update rule or loop
skeleton written down, pairs comparing orders of operations. If most
pairs stall, give the structure hint from the difficulty ladder
(below) whole-class rather than pair by pair.
Reveal and discuss — keep the reveal inside 10 minutes so the exit question still fits.


## Difficulty adaptation

- **Simplify.** Unroll one level of the call tree for them; they draw the second level. Then shrink the instance: run only the first
  step of doll with 5 layers and enlarge after success.
- **Extend.** Convert the recursion to a loop and compare the call stacks.
- **Hints (progressive).**
  1. *Re-orient:* "Define the subproblem in words (what does f(k) mean?)."
  2. *Structure:* "Write the base case and the self-reference before writing any code."
  3. *Near-answer:* the common wrong turn is 'a missing or wrong base case = infinite recursion' — undo
     that choice and re-derive the step it breaks.


## Reference solution (pseudocode)

FUNCTION count_layers(doll)
    IF doll is null THEN
        RETURN 0
    RETURN 1 + count_layers(doll.get('inner'))
// demo run: OUTPUT count_layers(doll)
// demo run: OUTPUT count_layers(null)


## Reference solution (Python)

```python
def count_layers(doll):
    if doll is None:
        return 0
    return 1 + count_layers(doll.get("inner"))

if __name__ == "__main__":
    doll = {"inner": {"inner": {"inner": None}}}
    print(count_layers(doll))          # 3
    print(count_layers(None))          # 0
```

The code is runnable as-is; executing the file prints the worked instance
so you can demo it live on the projector.


## Edge cases

- Base case: what is the smallest input, and does every path reach it?
- Empty or single-item input: decide what is returned and what must NOT be printed.
- Degenerate values: zero, negatives, and duplicates among the instance's data.


## Alternative approaches

- Write the base case in one sentence: 'a doll with nothing inside has 1 layer'.
- Iterative table versus recursive formulation: same recurrence, different order of filling it; the loop version cannot overflow any call stack.
- An explicit stack or a plain loop instead of the recursion: identical visits, no call-stack growth.


## Common pitfalls

- A missing or wrong base case = infinite recursion.
- Reporting the answer without the rule that produced it — the decision becomes uncheckable.


## Reveal script

1. Display the case and start the timer (5 minutes). While students work,
   circulate and read shoulders; pick one conventional attempt and one
   surprising one for the board.
2. Ask first: 'What is the smallest instance you can already solve — and how does a bigger one shrink onto it?'
3. Walk the worked instance (doll with 5 layers) on the board
   under the stated rule before any code appears; the class calls the
   next state at each step.
4. Surface the wrong turn directly: 'a missing or wrong base case = infinite recursion' — ask the room
   which quiet assumption it makes.
5. Show the reference Python, run it on the worked instance, and let the
   class compare with their own answers.
6. Close by naming the technique — "recursion and dynamic programming" — and hand over the
   discussion prompt: **"Where does the memory go in the memoised version, and what is the trade?"**


## Assessment use

- **Formative use.** discussion-grade: mark assumption quality and rule precision rather than the final number, and expect the full cycle to run long.
- **Mini-rubric (10 points).**
  - *Structure (2):* inputs, outputs, and the binding constraint named before any computation.
  - *Method (3):* the stated moves executed — especially "Find the base case(s) — the smallest inputs answered directly"
    and "Write the transition: how f(k) uses smaller answers".
  - *Result (3):* the worked instance computed correctly (count_layers: 0 for no doll, else 1 + inner; base case is the…),
    and the answer survives the smallest and the boundary input.
  - *Communication (2):* assumptions stated; the answer reports which rule produced it.
- **Follow-up check.** State the base case, then give an input that would never reach it if you can.


## Connections

- Discussion prompt for after the reveal: **"Where does the memory go in the memoised version, and what is the trade?"**
- This case belongs to lecture-22 as case cs-085 of four; it
  practises recursion, base case at beginner level.
- Case before: none (first of the lecture);
  case after: cs-086.


## Extension challenge

Convert the recursion to a loop and compare the call stacks.

Offer this only after the main cycle lands; it is enrichment, not
required content, and works as homework for fast finishers.

