# cs-073 — The Palindrome Walk (instructor)

| Field | Value |
| --- | --- |
| id | cs-073 |
| difficulty | Beginner |
| topics | two pointers, invariants |
| lecture | lecture-19 |
| estimated time | 5-minute attempt + 10-minute discussion |
| prerequisites | none |
| objective | state inputs, outputs, and assumptions, then solve a small instance by hand |

> **Instructor only.** Do not distribute to students or publish on the
> public site. The public build excludes this file and its text.

## Reasoning walkthrough

**Worked instance.** "Never odd or even" -> palindrome; "hello" -> not

**Expected result.** True / False via two pointers; invariant: everything outside [l, r] already verified

**Method — invariants and two pointers.** Walk the class through these moves:

1. Define the pointers and the region each is responsible for.
2. State the invariant in one sentence: what is always true?
3. Derive the movement rule from the invariant — never memorise it.
4. Argue termination: what strictly shrinks every iteration?

**Where this class gets stuck.** left and right walk inward, skipping non-alphanumerics; the loop ends when they meet or cross. Expect the first
five minutes to produce moving both pointers on a branch that should move one as the
most common wrong turn; plan the reveal around it.


## Five-minute teaching sequence

Minute 0–1 — **Read and analyse.** Expect inputs, outputs, and at least
one stated assumption named; the deciding quantity here is the input
rule. Watch for: retelling the story without extracting the decision.
Minute 1–3 — **Develop an approach.** Expect a rule or a first-step
computation on the instance; a correct rule with an arithmetic slip is
partial credit. Watch for: the input update that looks right but stalls on the smallest instance — have them test an interval of size two.
Minute 3–5 — **Compare and write.** Expect the update rule or loop
skeleton written down, pairs comparing orders of operations. If most
pairs stall, give the structure hint from the difficulty ladder
(below) whole-class rather than pair by pair.
Reveal and discuss — keep the reveal inside 10 minutes so the exit question still fits.


## Difficulty adaptation

- **Simplify.** Move physical markers along a printed row of values. Then shrink the instance: run only the first
  step of "Never odd or even" -> palindrome; "hello" -> not and enlarge after success.
- **Extend.** State the invariant in one sentence and point at the step that preserves it.
- **Hints (progressive).**
  1. *Re-orient:* "Define the pointers and the region each is responsible for."
  2. *Structure:* "Write the invariant as a sentence, then check every pointer move against it."
  3. *Near-answer:* the common wrong turn is 'moving both pointers on a branch that should move one' — undo
     that choice and re-derive the step it breaks.


## Reference solution (pseudocode)

FUNCTION is_palindrome_ptrs(s)
    (l, r) <- (0, LENGTH(s) - 1)
    WHILE l < r DO
        IF NOT (s[l] is alphanumeric) THEN
            l <- l + 1
        ELSE IF NOT (s[r] is alphanumeric) THEN
            r <- r - 1
        ELSE
            IF (s[l] lowercased) != (s[r] lowercased) THEN
                RETURN false
            ELSE
                l <- l + 1
                r <- r - 1
    RETURN true
// demo run: OUTPUT is_palindrome_ptrs('Never odd or even')
// demo run: OUTPUT is_palindrome_ptrs('hello')


## Reference solution (Python)

```python
def is_palindrome_ptrs(s):
    l, r = 0, len(s) - 1
    while l < r:
        if not s[l].isalnum():
            l += 1
        elif not s[r].isalnum():
            r -= 1
        elif s[l].lower() != s[r].lower():
            return False
        else:
            l += 1
            r -= 1
    return True

if __name__ == "__main__":
    print(is_palindrome_ptrs("Never odd or even"))
    print(is_palindrome_ptrs("hello"))
```

The code is runnable as-is; executing the file prints the worked instance
so you can demo it live on the projector.


## Edge cases

- Termination: name the update that guarantees the loop ends, and check it on the smallest input.
- Empty or single-item input: decide what is returned and what must NOT be printed.
- Degenerate values: zero, negatives, and duplicates among the instance's data.


## Alternative approaches

- Left and right walk inward, skipping non-alphanumerics; the loop ends when they meet or cross.
- Rewrite the while-loop as a bounded for-loop over the data (or vice versa) and compare which version makes termination obvious.
- A table filled by hand before any code: for the size of instance here it is both a solution and the test oracle.


## Common pitfalls

- Moving both pointers on a branch that should move one.
- An update path that never moves the state — the loop that never ends; test on an interval of size two.
- Reporting the answer without the rule that produced it — the decision becomes uncheckable.


## Reveal script

1. Display the case and start the timer (5 minutes). While students work,
   circulate and read shoulders; pick one conventional attempt and one
   surprising one for the board.
2. Ask first: 'What must stay true about the two regions after every single move?'
3. Walk the worked instance ("Never odd or even" -> palindrome; "hello" -> not) on the board
   under the stated rule before any code appears; the class calls the
   next state at each step.
4. Surface the wrong turn directly: 'moving both pointers on a branch that should move one' — ask the room
   which quiet assumption it makes.
5. Show the reference Python, run it on the worked instance, and let the
   class compare with their own answers.
6. Close by naming the technique — "invariants and two pointers" — and hand over the
   discussion prompt: **"Change one movement rule and predict the first input that breaks it."**


## Assessment use

- **Formative use.** discussion-grade: mark assumption quality and rule precision rather than the final number, and expect the full cycle to run long.
- **Mini-rubric (10 points).**
  - *Structure (2):* inputs, outputs, and the binding constraint named before any computation.
  - *Method (3):* the stated moves executed — especially "State the invariant in one sentence: what is always true?"
    and "Derive the movement rule from the invariant — never memorise it".
  - *Result (3):* the worked instance computed correctly (True / False via two pointers; invariant: everything outside…),
    and the answer survives the smallest and the boundary input.
  - *Communication (2):* assumptions stated; the answer reports which rule produced it.
- **Follow-up check.** Which update line guarantees termination? Delete it mentally: what goes wrong first?


## Connections

- Discussion prompt for after the reveal: **"Change one movement rule and predict the first input that breaks it."**
- This case belongs to lecture-19 as case cs-073 of four; it
  practises two pointers, invariants at intermediate level.
- Case before: none (first of the lecture);
  case after: cs-074.


## Extension challenge

State the invariant in one sentence and point at the step that preserves it.

Offer this only after the main cycle lands; it is enrichment, not
required content, and works as homework for fast finishers.

