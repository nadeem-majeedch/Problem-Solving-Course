# cs-056 — Pipelines as Functions (instructor)

| Field | Value |
| --- | --- |
| id | cs-056 |
| difficulty | Expert |
| topics | functions, composition |
| lecture | lecture-14 |
| estimated time | 5-minute attempt + 10-minute discussion |
| prerequisites | the full Block I-III toolbox and proof-shaped reasoning |
| objective | make and justify design decisions where the method itself is the challenge |

> **Instructor only.** Do not distribute to students or publish on the
> public site. The public build excludes this file and its text.

## Reasoning walkthrough

**Worked instance.** text '  the CAT, the dog!  '

**Expected result.** clean -> 'the CAT, the dog!'; tokenise -> ['the','CAT','the','dog']; count -> the:2, dog:1, cat:1

**Method — decomposition into functions.** Walk the class through these moves:

1. Find the nouns (data) and verbs (operations) in the task.
2. Give each verb one job, one clear contract: parameters in, return out.
3. Keep I/O at the edges; core functions compute and return.
4. Let the driver compose the pieces; check each piece alone.

**Where this class gets stuck.** Each stage returns data for the next; no stage prints. The pipeline is composition: report(count(tokenise(clean(t)))). Expect the first
five minutes to produce printing inside a 'compute' function makes it unusable twice as the
most common wrong turn; plan the reveal around it.


## Five-minute teaching sequence

Minute 0–1 — **Read and analyse.** Expect inputs, outputs, and at least
one stated assumption named; the deciding quantity here is the input
rule. Watch for: retelling the story without extracting the decision.
Minute 1–3 — **Develop an approach.** Expect a rule or a first-step
computation on the instance; a correct rule with an arithmetic slip is
partial credit. Watch for: reading an input key before it exists — the first occurrence needs the default path.
Minute 3–5 — **Compare and write.** Expect the update rule or loop
skeleton written down, pairs comparing orders of operations. If most
pairs stall, give the structure hint from the difficulty ladder
(below) whole-class rather than pair by pair.
Reveal and discuss — keep the reveal inside 10 minutes so the exit question still fits.


## Difficulty adaptation

- **Simplify.** Provide the function signatures; students fill in one body each. Then shrink the instance: run only the first
  step of text ' the CAT, the dog! ' and enlarge after success.
- **Extend.** Split one function into two and defend the new contract boundary.
- **Hints (progressive).**
  1. *Re-orient:* "Find the nouns (data) and verbs (operations) in the task."
  2. *Structure:* "Name the pieces by what they return, not by what they do internally."
  3. *Near-answer:* the common wrong turn is 'printing inside a 'compute' function makes it unusable twice' — undo
     that choice and re-derive the step it breaks.


## Reference solution (pseudocode)

FUNCTION clean(t)
    RETURN (' '.join((t split into words)) stripped ' ,.!?')
FUNCTION tokenise(t)
    RETURN {((w stripped '.,!?') lowercased) for each w in (t split into words)}
FUNCTION count_words(words)
    acc <- map{}
    FOR EACH w IN words DO
        acc[w] <- acc.get(w, default 0) + 1
    RETURN acc
// demo run: OUTPUT count_words(tokenise(clean(text)))


## Reference solution (Python)

```python
def clean(t):
    return " ".join(t.split()).strip(" ,.!?")

def tokenise(t):
    return [w.strip(".,!?").lower() for w in t.split()]

def count_words(words):
    acc = {}
    for w in words:
        acc[w] = acc.get(w, 0) + 1
    return acc

if __name__ == "__main__":
    text = "  the CAT, the dog!  "
    print(count_words(tokenise(clean(text))))
```

The code is runnable as-is; executing the file prints the worked instance
so you can demo it live on the projector.


## Edge cases

- First occurrence of a key not yet in the map — the default path must produce a defined answer.
- Empty or single-item input: decide what is returned and what must NOT be printed.
- Degenerate values: zero, negatives, and duplicates among the instance's data.


## Alternative approaches

- Each stage returns data for the next; no stage prints. The pipeline is composition: report(count(tokenise(clean(t)))).
- Loop-first formulation versus a comprehension/direct-expression formulation — same result, different emphasis on state versus intent.
- A plain list of (key, value) pairs instead of a map: fine at this size, but every lookup becomes a scan.
- **Cost.** Linear: one visit per item, a constant amount of work per visit; the alternatives shift exactly this cost.


## Common pitfalls

- Printing inside a 'compute' function makes it unusable twice.
- Reading a key before it exists (the first occurrence) instead of using the default path.
- Reporting the answer without the rule that produced it — the decision becomes uncheckable.


## Reveal script

1. Display the case and start the timer (5 minutes). While students work,
   circulate and read shoulders; pick one conventional attempt and one
   surprising one for the board.
2. Ask first: 'Which single responsibility resists decomposition — and what does each piece hand to the next?'
3. Walk the worked instance (text ' the CAT, the dog! ') on the board
   under the stated rule before any code appears; the class calls the
   next state at each step.
4. Surface the wrong turn directly: 'printing inside a 'compute' function makes it unusable twice' — ask the room
   which quiet assumption it makes.
5. Show the reference Python, run it on the worked instance, and let the
   class compare with their own answers.
6. Close by naming the technique — "decomposition into functions" — and hand over the
   discussion prompt: **"Which cut line would you move, and what argument justifies the boundary?"**


## Assessment use

- **Formative use.** discussion-grade: mark assumption quality and rule precision rather than the final number, and expect the full cycle to run long.
- **Mini-rubric (10 points).**
  - *Structure (2):* inputs, outputs, and the binding constraint named before any computation.
  - *Method (3):* the stated moves executed — especially "Give each verb one job, one clear contract: parameters in, return out"
    and "Keep I/O at the edges; core functions compute and return".
  - *Result (3):* the worked instance computed correctly (clean -> 'the CAT, the dog!'; tokenise ->…),
    and the answer survives the smallest and the boundary input.
  - *Communication (2):* assumptions stated; the answer reports which rule produced it.
- **Follow-up check.** A key appears that was never seen before: which line of your rule handles it, and what does it output?


## Connections

- Discussion prompt for after the reveal: **"Which cut line would you move, and what argument justifies the boundary?"**
- This case belongs to lecture-14 as case cs-056 of four; it
  practises functions, composition at advanced level.
- Case before: cs-055;
  case after: none (last of the lecture).


## Extension challenge

Split one function into two and defend the new contract boundary.

Offer this only after the main cycle lands; it is enrichment, not
required content, and works as homework for fast finishers.

