# cs-030 — The Age Gate (instructor)

| Field | Value |
| --- | --- |
| id | cs-030 |
| difficulty | Foundational |
| topics | testing, requirements |
| lecture | lecture-08 |
| estimated time | 5-minute attempt + 10-minute discussion |
| prerequisites | lectures 1-2 (problem framing, precise pseudocode) |
| objective | recognise the pattern, translate it into precise steps, and trace it correctly |

> **Instructor only.** Do not distribute to students or publish on the
> public site. The public build excludes this file and its text.

## Reasoning walkthrough

**Worked instance.** ages 11, 12 (Sat), 12 (Mon), 15 (Mon), 16 (Mon)

**Expected result.** refused; admitted; refused; admitted; admitted (weekday boundary is 16)

**Method — test design from requirements.** Walk the class through these moves:

1. Restate the requirement as input classes and their expected verdicts.
2. Add boundary cases: every 'at least', 'at most', and 'exactly'.
3. Add invalid and degenerate inputs — the requirement must say what happens.
4. Fix the oracle: where does the expected answer come from (spec, not code)?
5. Automate the table so the suite can be re-run after any change.

**Where this class gets stuck.** Boundary grid: day type x age around the thresholds; every cell is a test row. Expect the first
five minutes to produce writing tests by reading the code — the defect then defines truth as the
most common wrong turn; plan the reveal around it.


## Five-minute teaching sequence

Minute 0–1 — **Read and analyse.** Expect inputs, outputs, and at least
one stated assumption named; the deciding quantity here is the input
rule. Watch for: retelling the story without extracting the decision.
Minute 1–3 — **Develop an approach.** Expect a rule or a first-step
computation on the instance; a correct rule with an arithmetic slip is
partial credit. Watch for: a rule that reads correctly but computes on the wrong quantity in the input data.
Minute 3–5 — **Compare and write.** Expect the update rule or loop
skeleton written down, pairs comparing orders of operations. If most
pairs stall, give the structure hint from the difficulty ladder
(below) whole-class rather than pair by pair.
Reveal and discuss — keep the reveal inside 10 minutes so the exit question still fits.


## Difficulty adaptation

- **Simplify.** Start from a given test table with one deliberately impossible row to find. Then shrink the instance: run only the first
  step of ages 11, 12 (Sat), 12 (Mon), 15 (Mon), 16 (Mon) and enlarge after success.
- **Extend.** Add two boundary rows to your test table and justify each.
- **Hints (progressive).**
  1. *Re-orient:* "Restate the requirement as input classes and their expected verdicts."
  2. *Structure:* "Aim at boundaries and rule intersections, not at random values."
  3. *Near-answer:* the common wrong turn is 'writing tests by reading the code — the defect then defines truth' — undo
     that choice and re-derive the step it breaks.


## Reference solution (pseudocode)

FUNCTION admits(age, weekend)
    limit <- (12 if weekend else 16)
    RETURN age >= limit


## Reference solution (Python)

```python
def admits(age, weekend):
    limit = 12 if weekend else 16
    return age >= limit

if __name__ == "__main__":
    for age, wk in ((11, True), (12, True), (12, False), (15, False), (16, False)):
        print(age, wk, admits(age, wk))
```

The code is runnable as-is; executing the file prints the worked instance
so you can demo it live on the projector.


## Edge cases

- Empty or single-item input: decide what is returned and what must NOT be printed.
- Degenerate values: zero, negatives, and duplicates among the instance's data.


## Alternative approaches

- Boundary grid: day type x age around the thresholds; every cell is a test row.
- Loop-first formulation versus a comprehension/direct-expression formulation — same result, different emphasis on state versus intent.
- A table filled by hand before any code: for the size of instance here it is both a solution and the test oracle.


## Common pitfalls

- Writing tests by reading the code — the defect then defines truth.
- Reporting the answer without the rule that produced it — the decision becomes uncheckable.


## Reveal script

1. Display the case and start the timer (5 minutes). While students work,
   circulate and read shoulders; pick one conventional attempt and one
   surprising one for the board.
2. Ask first: 'Which input would you hand this program to make it fail fastest?'
3. Walk the worked instance (ages 11, 12 (Sat), 12 (Mon), 15 (Mon), 16 (Mon)) on the board
   under the stated rule before any code appears; the class calls the
   next state at each step.
4. Surface the wrong turn directly: 'writing tests by reading the code — the defect then defines truth' — ask the room
   which quiet assumption it makes.
5. Show the reference Python, run it on the worked instance, and let the
   class compare with their own answers.
6. Close by naming the technique — "test design from requirements" — and hand over the
   discussion prompt: **"Which row in your table would you defend hardest, and why?"**


## Assessment use

- **Formative use.** discussion-grade: mark assumption quality and rule precision rather than the final number, and expect the full cycle to run long.
- **Mini-rubric (10 points).**
  - *Structure (2):* inputs, outputs, and the binding constraint named before any computation.
  - *Method (3):* the stated moves executed — especially "Add boundary cases: every 'at least', 'at most', and 'exactly'"
    and "Add invalid and degenerate inputs — the requirement must say what happens".
  - *Result (3):* the worked instance computed correctly (refused; admitted; refused; admitted; admitted (weekday…),
    and the answer survives the smallest and the boundary input.
  - *Communication (2):* assumptions stated; the answer reports which rule produced it.
- **Follow-up check.** State the deciding rule in one sentence and apply it to a half-size version of the worked instance.


## Connections

- Discussion prompt for after the reveal: **"Which row in your table would you defend hardest, and why?"**
- This case belongs to lecture-08 as case cs-030 of four; it
  practises testing, requirements at beginner level.
- Case before: cs-029;
  case after: cs-031.


## Extension challenge

Add two boundary rows to your test table and justify each.

Offer this only after the main cycle lands; it is enrichment, not
required content, and works as homework for fast finishers.

