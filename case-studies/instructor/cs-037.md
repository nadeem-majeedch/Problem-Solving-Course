# cs-037 — Username Auditor (instructor)

| Field | Value |
| --- | --- |
| id | cs-037 |
| difficulty | Beginner |
| topics | strings, validation |
| lecture | lecture-10 |
| estimated time | 5-minute attempt + 10-minute discussion |
| prerequisites | none |
| objective | state inputs, outputs, and assumptions, then solve a small instance by hand |

> **Instructor only.** Do not distribute to students or publish on the
> public site. The public build excludes this file and its text.

## Reasoning walkthrough

**Worked instance.** "ada_2019" ok; "ada__love" fail (consecutive underscores); "9lives" fail; "ok" fail (short)

**Expected result.** demo verdicts: 'ada_2019 -> pass', 'ada__love -> fail: consecutive underscores', '9lives -> fail: starts with letter', 'ok -> fail: length'

**Method — string transformation and checking.** Walk the class through these moves:

1. Decide the unit of work: the whole string, words, or characters.
2. Normalise first (case, separators) if the comparison should ignore them.
3. Build or check character by character, guarding indices.
4. State the output for every verdict path (match, no match, degenerate).

**Where this class gets stuck.** Check rules in an order that produces the most useful message first; walk once, flag once. Expect the first
five minutes to produce strings are immutable: 'modifying' one builds a new string each time as the
most common wrong turn; plan the reveal around it.


## Five-minute teaching sequence

Minute 0–1 — **Read and analyse.** Expect inputs, outputs, and at least
one stated assumption named; the deciding quantity here is the input
rule. Watch for: retelling the story without extracting the decision.
Minute 1–3 — **Develop an approach.** Expect a rule or a first-step
computation on the instance; a correct rule with an arithmetic slip is
partial credit. Watch for: a rule that reads correctly but computes on the wrong quantity in the input data.
Minute 3–5 — **Compare and write.** Expect the update rule or loop
skeleton written down, pairs comparing orders of operations. If most
pairs stall, give the structure hint from the difficulty ladder
(below) whole-class rather than pair by pair.
Reveal and discuss — keep the reveal inside 10 minutes so the exit question still fits.


## Difficulty adaptation

- **Simplify.** Use a five-character string and mark the decision on each character. Then shrink the instance: run only the first
  step of "ada_2019" ok; "ada__love" fail (consecutive underscores);… and enlarge after success.
- **Extend.** Normalise the input differently and show the answer does (or does not) change.
- **Hints (progressive).**
  1. *Re-orient:* "Decide the unit of work: the whole string, words, or characters."
  2. *Structure:* "Normalise first (case, separators), then decide; two rules stop fighting once the text is normalised."
  3. *Near-answer:* the common wrong turn is 'strings are immutable: 'modifying' one builds a new string each time' — undo
     that choice and re-derive the step it breaks.


## Reference solution (pseudocode)

FUNCTION audit(username)
    IF NOT 5 <= LENGTH(username) <= 16 THEN
        RETURN 'fail: length'
    IF NOT (username[0] is a letter) THEN
        RETURN 'fail: starts with letter'
    FOR EACH ch IN username DO
        IF NOT ((ch is alphanumeric)) OR (ch = '_') THEN
            RETURN 'fail: character set'
    IF '__' in username THEN
        RETURN 'fail: consecutive underscores'
    RETURN 'pass'


## Reference solution (Python)

```python
def audit(username):
    if not (5 <= len(username) <= 16):
        return "fail: length"
    if not username[0].isalpha():
        return "fail: starts with letter"
    for ch in username:
        if not (ch.isalnum() or ch == "_"):
            return "fail: character set"
    if "__" in username:
        return "fail: consecutive underscores"
    return "pass"

if __name__ == "__main__":
    for u in ("ada_2019", "ada__love", "9lives", "ok"):
        print(u, "->", audit(u))
```

The code is runnable as-is; executing the file prints the worked instance
so you can demo it live on the projector.


## Edge cases

- Empty or single-item input: decide what is returned and what must NOT be printed.
- Degenerate values: zero, negatives, and duplicates among the instance's data.


## Alternative approaches

- Check rules in an order that produces the most useful message first; walk once, flag once.
- Loop-first formulation versus a comprehension/direct-expression formulation — same result, different emphasis on state versus intent.
- A table filled by hand before any code: for the size of instance here it is both a solution and the test oracle.
- **Cost.** Linear: one visit per item, a constant amount of work per visit; the alternatives shift exactly this cost.


## Common pitfalls

- Strings are immutable: 'modifying' one builds a new string each time.
- Reporting the answer without the rule that produced it — the decision becomes uncheckable.


## Reveal script

1. Display the case and start the timer (5 minutes). While students work,
   circulate and read shoulders; pick one conventional attempt and one
   surprising one for the board.
2. Ask first: 'What is the smallest unit of text a decision attaches to — character, token, or whole string?'
3. Walk the worked instance ("ada_2019" ok; "ada__love" fail (consecutive underscores); "9lives" fail; "ok" fail…) on the board
   under the stated rule before any code appears; the class calls the
   next state at each step.
4. Surface the wrong turn directly: 'strings are immutable: 'modifying' one builds a new string each time' — ask the room
   which quiet assumption it makes.
5. Show the reference Python, run it on the worked instance, and let the
   class compare with their own answers.
6. Close by naming the technique — "string transformation and checking" — and hand over the
   discussion prompt: **"Swap your example with a neighbour: does their edge case break your solution?"**


## Assessment use

- **Formative use.** discussion-grade: mark assumption quality and rule precision rather than the final number, and expect the full cycle to run long.
- **Mini-rubric (10 points).**
  - *Structure (2):* inputs, outputs, and the binding constraint named before any computation.
  - *Method (3):* the stated moves executed — especially "Normalise first (case, separators) if the comparison should ignore them"
    and "Build or check character by character, guarding indices".
  - *Result (3):* the worked instance computed correctly (verdicts as above; rules: 5-16 chars, letters/digits/underscore…),
    and the answer survives the smallest and the boundary input.
  - *Communication (2):* assumptions stated; the answer reports which rule produced it.
- **Follow-up check.** State the deciding rule in one sentence and apply it to a half-size version of the worked instance.


## Connections

- Discussion prompt for after the reveal: **"Swap your example with a neighbour: does their edge case break your solution?"**
- This case belongs to lecture-10 as case cs-037 of four; it
  practises strings, validation at beginner level.
- Case before: none (first of the lecture);
  case after: cs-038.


## Extension challenge

Normalise the input differently and show the answer does (or does not) change.

Offer this only after the main cycle lands; it is enrichment, not
required content, and works as homework for fast finishers.

