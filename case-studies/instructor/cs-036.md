# cs-036 — Rainfall Windows (instructor)

| Field | Value |
| --- | --- |
| id | cs-036 |
| difficulty | Advanced |
| topics | aggregation, windows |
| lecture | lecture-09 |
| estimated time | 5-minute attempt + 10-minute discussion |
| prerequisites | the full Block I-III toolbox and proof-shaped reasoning |
| objective | make and justify design decisions where the method itself is the challenge |

> **Instructor only.** Do not distribute to students or publish on the
> public site. The public build excludes this file and its text.

## Reasoning walkthrough

**Worked instance.** rain [3, 5, 2, 8, 1, 4], window k = 3

**Expected result.** window maxima: 5, 8, 8, 8 — for windows [3,5,2], [5,2,8], [2,8,1], [8,1,4]

**Method — aggregation over a sequence.** Walk the class through these moves:

1. Restate what is being accumulated and over what sequence.
2. Choose the accumulator(s) and their correct starting values.
3. Decide the update rule and any filter applied before updating.
4. Decide what is reported at the end (and its type).
5. Check the accumulator initialisation against the smallest input.

**Where this class gets stuck.** Naive recomputes each window in O(nk). One step better: keep the running max with a decreasing deque - or at least avoid re-scanning when the old max leaves. Expect the first
five minutes to produce initialising best to 0 breaks when all values are negative or below target as the
most common wrong turn; plan the reveal around it.


## Five-minute teaching sequence

Minute 0–1 — **Read and analyse.** Expect inputs, outputs, and at least
one stated assumption named; the deciding quantity here is the input
rule. Watch for: retelling the story without extracting the decision.
Minute 1–3 — **Develop an approach.** Expect a rule or a first-step
computation on the instance; a correct rule with an arithmetic slip is
partial credit. Watch for: the input update that looks right but stalls on the smallest instance — have them test an interval of size two.
Minute 3–5 — **Compare and write.** Expect the update rule or loop
skeleton written down, pairs comparing orders of operations. If most
pairs stall, give the structure hint from the difficulty ladder
(below) whole-class rather than pair by pair.
Reveal and discuss — keep the reveal inside 10 minutes so the exit question still fits.


## Difficulty adaptation

- **Simplify.** Fill the accumulator table for the first two items together; pairs complete the rest. Then shrink the instance: run only the first
  step of rain [3, 5, 2, 8, 1, 4], window k = 3 and enlarge after success.
- **Extend.** Recompute all aggregates in a second pass and argue which pass you would keep.
- **Hints (progressive).**
  1. *Re-orient:* "Restate what is being accumulated and over what sequence."
  2. *Structure:* "Choose the accumulator and its starting value first; the loop body writes itself after that."
  3. *Near-answer:* the common wrong turn is 'initialising best to 0 breaks when all values are negative or below target' — undo
     that choice and re-derive the step it breaks.


## Reference solution (pseudocode)

FUNCTION window_maxima(rain, k)
    dq <- deque()
    out <- []
    FOR EACH (i, v) IN ENUMERATE(rain) DO
        WHILE (dq) AND (rain[dq[-1]] <= v) DO
            dq.pop()
        append i to dq
        IF dq[0] <= i - k THEN
            dq.popleft()
        IF i >= k - 1 THEN
            append rain[dq[0]] to out
    RETURN out
// demo run: OUTPUT window_maxima([3, 5, 2, 8, 1, 4], 3)


## Reference solution (Python)

```python
def window_maxima(rain, k):
    from collections import deque
    dq = deque()  # indices, values decreasing
    out = []
    for i, v in enumerate(rain):
        while dq and rain[dq[-1]] <= v:
            dq.pop()
        dq.append(i)
        if dq[0] <= i - k:
            dq.popleft()
        if i >= k - 1:
            out.append(rain[dq[0]])
    return out

if __name__ == "__main__":
    print(window_maxima([3, 5, 2, 8, 1, 4], 3))   # [5, 8, 8, 8]
```

The code is runnable as-is; executing the file prints the worked instance
so you can demo it live on the projector.


## Edge cases

- Termination: name the update that guarantees the loop ends, and check it on the smallest input.
- Empty or single-item input: decide what is returned and what must NOT be printed.
- Boundary positions: the first item, the last item, and the position one past each end (the i-1 / i+1 arithmetic).


## Alternative approaches

- Naive recomputes each window in O(nk). One step better: keep the running max with a decreasing deque - or at least avoid re-scanning when the old max leaves.
- Several passes with built-in helpers (a sum here, a max there) instead of one pass with several accumulators — clearer to read, more visits over the data.
- A table filled by hand before any code: for the size of instance here it is both a solution and the test oracle.
- **Cost.** Linear: one visit per item, a constant amount of work per visit; the alternatives shift exactly this cost.


## Common pitfalls

- Initialising best to 0 breaks when all values are negative or below target.
- An update path that never moves the state — the loop that never ends; test on an interval of size two.
- Mutating the caller's list when the result was meant to be a fresh one — the demo where the input 'changes by itself'.
- Off-by-one at the boundary: the update that is correct for the middle of the data and wrong at the two ends.
- Reporting the answer without the rule that produced it — the decision becomes uncheckable.


## Reveal script

1. Display the case and start the timer (5 minutes). While students work,
   circulate and read shoulders; pick one conventional attempt and one
   surprising one for the board.
2. Ask first: 'What must the loop remember between two items — and what can it safely forget?'
3. Walk the worked instance (rain [3, 5, 2, 8, 1, 4], window k = 3) on the board
   under the stated rule before any code appears; the class calls the
   next state at each step.
4. Surface the wrong turn directly: 'initialising best to 0 breaks when all values are negative or below target' — ask the room
   which quiet assumption it makes.
5. Show the reference Python, run it on the worked instance, and let the
   class compare with their own answers.
6. Close by naming the technique — "aggregation over a sequence" — and hand over the
   discussion prompt: **"Where exactly would this solution break first if the input grew to a million items — and would anything break at all?"**


## Assessment use

- **Formative use.** discussion-grade: mark assumption quality and rule precision rather than the final number, and expect the full cycle to run long.
- **Mini-rubric (10 points).**
  - *Structure (2):* inputs, outputs, and the binding constraint named before any computation.
  - *Method (3):* the stated moves executed — especially "Choose the accumulator(s) and their correct starting values"
    and "Decide the update rule and any filter applied before updating".
  - *Result (3):* the worked instance computed correctly (window maxima: 5, 8, 8, 8 — for windows [3,5,2], [5,2,8]…),
    and the answer survives the smallest and the boundary input.
  - *Communication (2):* assumptions stated; the answer reports which rule produced it.
- **Follow-up check.** Which update line guarantees termination? Delete it mentally: what goes wrong first?


## Connections

- Discussion prompt for after the reveal: **"Where exactly would this solution break first if the input grew to a million items — and would anything break at all?"**
- This case belongs to lecture-09 as case cs-036 of four; it
  practises aggregation, windows at advanced level.
- Case before: cs-035;
  case after: none (last of the lecture).


## Extension challenge

Recompute all aggregates in a second pass and argue which pass you would keep.

Offer this only after the main cycle lands; it is enrichment, not
required content, and works as homework for fast finishers.

