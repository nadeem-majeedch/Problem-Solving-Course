# cs-058 — Team Photo Lineups (instructor)

| Field | Value |
| --- | --- |
| id | cs-058 |
| difficulty | Foundational |
| topics | permutations, constraints |
| lecture | lecture-15 |
| estimated time | 5-minute attempt + 10-minute discussion |
| prerequisites | lectures 1-2 (problem framing, precise pseudocode) |
| objective | recognise the pattern, translate it into precise steps, and trace it correctly |

> **Instructor only.** Do not distribute to students or publish on the
> public site. The public build excludes this file and its text.

## Reasoning walkthrough

**Worked instance.** 5 students; students P and Q refuse to stand together

**Expected result.** 5! = 120; together (block): 4! x 2 = 48; apart: 120 - 48 = 72

**Method — counting and probability.** Walk the class through these moves:

1. Frame the question: how many outcomes, and how many favourable?
2. Decide ordered vs unordered, with vs without replacement — in words first.
3. Compute with the multiplication/addition principles (or complements).
4. Check the count by brute-force enumeration when small.

**Where this class gets stuck.** Count ALL, count BAD (glue P and Q into one block, 2 orders inside), subtract. Expect the first
five minutes to produce adding situations that multiply (wearing a shirt and trousers) as the
most common wrong turn; plan the reveal around it.


## Five-minute teaching sequence

Minute 0–1 — **Read and analyse.** Expect inputs, outputs, and at least
one stated assumption named; the deciding quantity here is the input
rule. Watch for: retelling the story without extracting the decision.
Minute 1–3 — **Develop an approach.** Expect a rule or a first-step
computation on the instance; a correct rule with an arithmetic slip is
partial credit. Watch for: a rule that reads correctly but computes on the wrong quantity in the input data.
Minute 3–5 — **Compare and write.** Expect the update rule or loop
skeleton written down, pairs comparing orders of operations. If most
pairs stall, give the structure hint from the difficulty ladder
(below) whole-class rather than pair by pair.
Reveal and discuss — keep the reveal inside 10 minutes so the exit question still fits.


## Difficulty adaptation

- **Simplify.** List the outcomes for a reduced instance and count them by hand. Then shrink the instance: run only the first
  step of 5 students; students P and Q refuse to stand together and enlarge after success.
- **Extend.** Re-derive the count with a different method and reconcile the answers.
- **Hints (progressive).**
  1. *Re-orient:* "Frame the question: how many outcomes, and how many favourable?."
  2. *Structure:* "Fix the sample space first; every shortcut is a claim about that space."
  3. *Near-answer:* the common wrong turn is 'adding situations that multiply (wearing a shirt and trousers)' — undo
     that choice and re-derive the step it breaks.


## Reference solution (pseudocode)

FUNCTION count_apart(n, a, b)
    people <- LIST(range 0..n)
    total <- 0
    FOR EACH p IN permutations(people) DO
        (ia, ib) <- (p.index(a), p.index(b))
        IF ABS(ia - ib) > 1 THEN
            total <- total + 1
    RETURN total
// demo run: OUTPUT count_apart()


## Reference solution (Python)

```python
from itertools import permutations

def count_apart(n=5, a=0, b=1):
    people = list(range(n))
    total = 0
    for p in permutations(people):
        ia, ib = p.index(a), p.index(b)
        if abs(ia - ib) > 1:
            total += 1
    return total

if __name__ == "__main__":
    print(count_apart())   # 72
```

The code is runnable as-is; executing the file prints the worked instance
so you can demo it live on the projector.


## Edge cases

- Empty or single-item input: decide what is returned and what must NOT be printed.
- Degenerate values: zero, negatives, and duplicates among the instance's data.


## Alternative approaches

- Count ALL, count BAD (glue P and Q into one block, 2 orders inside), subtract.
- Loop-first formulation versus a comprehension/direct-expression formulation — same result, different emphasis on state versus intent.
- A table filled by hand before any code: for the size of instance here it is both a solution and the test oracle.
- **Cost.** Linear: one visit per item, a constant amount of work per visit; the alternatives shift exactly this cost.


## Common pitfalls

- Adding situations that multiply (wearing a shirt and trousers).
- Reporting the answer without the rule that produced it — the decision becomes uncheckable.


## Reveal script

1. Display the case and start the timer (5 minutes). While students work,
   circulate and read shoulders; pick one conventional attempt and one
   surprising one for the board.
2. Ask first: 'Are you counting arrangements, selections, or successes — and does order matter here?'
3. Walk the worked instance (5 students; students P and Q refuse to stand together) on the board
   under the stated rule before any code appears; the class calls the
   next state at each step.
4. Surface the wrong turn directly: 'adding situations that multiply (wearing a shirt and trousers)' — ask the room
   which quiet assumption it makes.
5. Show the reference Python, run it on the worked instance, and let the
   class compare with their own answers.
6. Close by naming the technique — "counting and probability" — and hand over the
   discussion prompt: **"Where exactly did the wrong counts double-count — point at the pair."**


## Assessment use

- **Formative use.** discussion-grade: mark assumption quality and rule precision rather than the final number, and expect the full cycle to run long.
- **Mini-rubric (10 points).**
  - *Structure (2):* inputs, outputs, and the binding constraint named before any computation.
  - *Method (3):* the stated moves executed — especially "Decide ordered vs unordered, with vs without replacement — in words first"
    and "Compute with the multiplication/addition principles (or complements)".
  - *Result (3):* the worked instance computed correctly (5! = 120; together (block): 4! x 2 = 48; apart: 120 - 48 = 72),
    and the answer survives the smallest and the boundary input.
  - *Communication (2):* assumptions stated; the answer reports which rule produced it.
- **Follow-up check.** State the deciding rule in one sentence and apply it to a half-size version of the worked instance.


## Connections

- Discussion prompt for after the reveal: **"Where exactly did the wrong counts double-count — point at the pair."**
- This case belongs to lecture-15 as case cs-058 of four; it
  practises permutations, constraints at beginner level.
- Case before: cs-057;
  case after: cs-059.


## Extension challenge

Re-derive the count with a different method and reconcile the answers.

Offer this only after the main cycle lands; it is enrichment, not
required content, and works as homework for fast finishers.

